import { GuildSettings } from './GuildSettings';
import { SoundCommand } from './SoundCommand';
import { Statistic, Usage } from './Statistic';
import { Vote } from './Vote';

export const entities = [GuildSettings, SoundCommand, Statistic, Usage, Vote];
